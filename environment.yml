name: nba-stats
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - numpy
  - pandas
  - scipy
  - scikit-learn
  - matplotlib
  - requests
  - beautifulsoup4
  - lxml
  - tqdm
  - ipykernel
  - jupyter
  - pip:
      # Optional: add any pip-only deps here if you discover you need them
      # - some-package
